动态语言与静态语言
语言的静态与动态是，指一个变量的类型到底是一开始定义就确定的（静态的）还是直到赋值运行时才确定的
（动态的）。

面向对象的思想是万物皆是类，所有的类都是从object类继承而来的。
假如定义了一个函数
静态：
func（object obj）
{
    obj.run();
}
动态：
func（object）
    object.run()


静态语言在调用函数使必须传入object类型的对象或者其子类,然后调用其定义的run方法。
动态语言则只要你传入一个对象（声明函数时并没有指定对象类型），只要这个对象有一个run方法，那就是可
以执行的。


也就是说假如我需要一个鸭子，只要你给我的对象看上去像鸭子，跑起来像鸭子，能满足我的需求，那它就是只鸭子。
伟大的邓小平说过，不管黑猫白猫，抓到老鼠的就是好猫，也是一个意思。

我是谁的鸭子,不，我谁都不是
fsafasfsafsaasffffffffffffffffffffff