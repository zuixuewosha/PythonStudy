python支持通过下标获取列表、元组的元素，但大量有规律截取元素时，使用切片更加的有效
list=["aa","bb","cc",....]
语法：list[startpos:endpos:interval]
startpos：开始的下标(包括)
endpos：结束的下标(不包括)
interval：切片的间隔

当interval小于0时，startpos默认为len（array）-1，endpos默认为开头之前。
当interval大于0时，startpos默认为0，endpos默认为最末之后。
当interval未给出时：interval默认为1


字符串、tuple等也可以切片，切片后返回对应的tuple和字符串